<!DOCTYPE html>
<html>
  <head>
    <title>
      <%= content_for?(:title) ? yield(:title) : "Udemy Rails App" %>
    </title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="author" content="Alejandro Toledo">
    <meta name="keywords" content="online education, course platform, video tutorials, udemy clone">

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <%= stylesheet_link_tag "tailwind", "inter-font", "data-turbo-track": "reload" %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>

    <%= favicon_link_tag 'ruby-icon.png' %>
  </head>

  <%= render "layouts/header" %>
  <%= render "layouts/messages" %>
  <body>
    <main class="px-5 mx-auto mt-6 ">
      <%= yield %>
      <%= turbo_frame_tag "modal" %>
      <%= turbo_frame_tag "edit-modal" %>
    </main>
  </body>

  <footer class="w-full h-20 pt-10 mt-10 text-sm text-center text-white bg-blue-400">Alejandro Toledo: Developer & Creator</footer>
</html>
