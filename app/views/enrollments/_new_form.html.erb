<%= form_with(model: enrollment, class: "contents md:min-w-[400px]") do |form| %>
  <% if enrollment.errors.any? %>
    <div id="error_explanation" class="px-3 py-2 mt-3 font-medium text-red-500 rounded-lg bg-red-50 ">
      <h2><%= pluralize(enrollment.errors.count, "error") %> prohibited this enrollment from being saved:</h2>
      <ul>
        <% enrollment.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>
  <section class="my-5">
    <p>Course: <%= course.title.capitalize %></p>
    <p>Id: <%= course.id %></p>
    <p>User: <%= current_user.email %></p>
    <p>Price: <%= number_to_currency(course.price) %></p>
  </section>
  <div class="my-5">
    <%= form.hidden_field :course_id, value: course.slug %>
    <%= form.hidden_field :user_id, value: current_user.id %>
  </div>
  <div class="inline">
    <%= form.submit "Buy this course", class: "rounded-lg py-3 px-5 bg-blue-600 text-white inline-block font-medium cursor-pointer" %>
  </div>
<% end %>
